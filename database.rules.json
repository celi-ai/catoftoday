{
  "database": {
    "rules": {
      "patCount": {
        ".read": true,
        ".write": true
      },
      "$other": {
        ".read": false,
        ".write": false
      }
    }
  },
  "storage": {
    "rules": "rules_version = '2';\nservice firebase.storage {\n  match /b/{bucket}/o {\n    match /{allPaths=**} {\n      allow read, write: if true;\n    }\n  }\n}"
  }
}